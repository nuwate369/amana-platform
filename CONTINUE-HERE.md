# نقطة المتابعة — منصة أمانة (آخر تحديث: 19‑07‑2026 — جلسة مساء)

مرجع تسليم بين الأجهزة. **اقرأه أولًا عند استئناف العمل.**

> ✅ **حالة Git:** كل العمل مُثبَّت ومدفوع على `main` (hook تلقائي). آخر التعديلات: إصلاحات بيئة التطوير (منافذ مخصصة، ملفات .env)، توجيه تسجيل دخول الإدارة، وإصلاح منطق حالة اتصال السائقة (زر متصلة/غير متصلة) أثناء الرحلة الجارية.

---

## 🖥️ على جهاز المكتب — قبل أي شيء
```bash
cd <مسار>/amana-platform
git pull
npm install            # postinstall يطبّق رقعة css-interop تلقائيًّا
```
**ثم أعِد إنشاء ملفّي `.env`** (متجاهَلان في Git — لا يُسحبان):
- `apps/driver/.env` و`apps/passenger/.env`، تم إنشاؤها مسبقاً وتتضمن:
  `EXPO_PUBLIC_SUPABASE_URL` · `EXPO_PUBLIC_SUPABASE_ANON_KEY` · `EXPO_PUBLIC_MAPBOX_TOKEN` (pk.)
- `apps/admin/.env.local`: `NEXT_PUBLIC_SUPABASE_URL` · `NEXT_PUBLIC_SUPABASE_ANON_KEY` · `SUPABASE_SERVICE_ROLE_KEY` · `NEXT_PUBLIC_MAPBOX_TOKEN` (pk.)

---

## 🔴 إجراءات معلّقة (نفّذها لترى النتيجة)

### 1) هجرات Supabase — SQL Editor → Run (بالترتيب، idempotent)
| الهجرة | الحالة | الغرض |
|---|---|---|
| 0029–0033 | ✅ شُغّلت | Realtime + الحضور + قبول الرحلة + تنبيهات التذاكر + الدفع |
| **`0034_user_activation.sql`** | ⚠️ **شغّلها** | تفعيل المستخدم + `email_confirmed_at` + status |
| **`0035_ride_arrival.sql`** | ⚠️ **شغّلها** | عمود `driver_arrived_at` (خطوة وصول السائقة) |
| **`0036_vehicle_class.sql`** | ⚠️ **شغّلها** | عمود فئة المركبة للسائقة والرحلات |
| **`0037_announcements.sql`** | ⚠️ **شغّلها** | جدول الإشعارات (للوحة الإدارة) |
| **`0038_announcements_expiration.sql`** | ⚠️ **شغّلها** | تاريخ انتهاء للإشعارات (`expires_at`) |

> بدون 0035 لن يظهر زر «وصلت لنقطة الالتقاط». بدون 0034 تظهر حالات المستخدمين خطأً.

### 2) Vercel (لوحة الإدارة على الإنتاج)
أضِف `NEXT_PUBLIC_MAPBOX_TOKEN` (pk.) في Vercel → مشروع الأدمن → Settings → Environment Variables → Redeploy. (وإلا خريطة الرحلات فارغة على الإنتاج فقط.)

### 3) إعادة بناء APK (عند الحاجة فقط — تغييرات الكود اليومية فورية عبر dev‑client)
- **الراكب:** أُضيفت `expo-dev-client` → **يحتاج بناءً جديدًا** ليظهر مشغّل dev.
- **الشعار الجديد** (كلا التطبيقين): يظهر فقط بعد بناء جديد.
```bash
cd apps/passenger && eas build --profile development --platform android
cd apps/driver    && eas build --profile development --platform android
```

---

## ✅ ما أُنجز (الجلسة الحالية)

**1. فئة المركبة (Vehicle Class):**
- الراكب: يختار الفئة وتُحفظ فعلًا في الرحلة (`rides.requested_class`).
- السائق: يُدخل الفئة في التوثيق KYC (standard/premium/group)، وتُحفظ في `drivers.vehicle_class`. 
- عرض الفئة المطلوبة للسائق على بطاقة الطلب الجديد.

**2. إشعارات لوحة الإدارة (`announcements`):**
- بناء جدول للإشعارات الحقيقية (`0037` و `0038`).
- صفحة إشعارات متكاملة (فلاتر، إحصاءات، نموذج إرسال لـ الكل/الركاب/السائقات/مستخدم محدد).
- **جديد:** منطق تاريخ الانتهاء `expires_at` بحيث لا يظهر الإشعار للمستخدمين الجدد بعد انتهائه.

**3. الدعم الذكي للراكب (Groq AI):**
- بناء نظام دعم ذكي للراكب (محادثات + تصعيد لتذاكر حقيقية) بنفس بنية السائق.

**4. تحسينات الخريطة والتتبع:**
- أيقونة السائقة سيارة بدل نقطة، مع رسم مسار (Route) كحلي/أبيض بين نقطة الالتقاط والوجهة (عبر Mapbox Directions).
- إصلاحات UI الراكب: هامش safe-area في التتبع، زر الإشعارات فوق الخريطة، إخفاء أزرار الشريط السفلي (إشعارات/حسابي) لمنع التكرار، ودعم لوحة المفاتيح (KeyboardAvoidingView) في تسجيل الدخول.

**7. إخفاء أيقونات الشريط السفلي المكررة (الراكب):**
- تبويبا «الإشعارات» و«حسابي» في `app/(tabs)/_layout.tsx` محدَّثان بـ `href: null` — لا يظهران في الشريط السفلي.
- الأيقونتان موجودتان في الشريط العلوي للرئيسية فقط، لا تكرار.

**8. إصلاحات التشغيل وبيئة التطوير (أحدث التعديلات):**
- **ملفات البيئة:** تم استرجاع مفاتيح `EXPO_PUBLIC_MAPBOX_TOKEN` و `EXPO_PUBLIC_SUPABASE_URL` في ملفات `.env` لكل تطبيق لحل مشكلة عدم ظهور الخريطة وانهيار Supabase.
- **إعادة توجيه الإدارة:** تم تعديل توجيه لوحة الإدارة (`admin`) بعد الدخول الناجح لينتقل إلى `/dashboard` مباشرة (يعمل محلياً وعلى `amana.nuwate.com`).
- **منافذ التشغيل:** تحديث `package.json` ليستخدم `8082` للراكب و `8083` للسائق مع إضافة مسح الكاش `-c` افتراضياً.
- **منطق حالة الاتصال:** تم إصلاح التناقض في تطبيق السائق (عرض "غير متصلة" رغم وجود رحلة جارية). الآن عند وجود رحلة جارية، يُجبر الزر على الظهور بلون أخضر (متصلة) ويُعطل (disabled) لمنع الإغلاق الخطأ.

---

## ⏳ المتبقّي / مؤجّل
- **أزرار placeholder** في تتبّع الراكب: «اتصال»/«مراسلة»/SOS بلا وظيفة (تحتاج أرقام/محادثة).
- **محادثة راكبة ↔ سائقة** — غير مبنيّة.
- **بوابة دفع حقيقية** — مؤجّلة (تحتاج سجلًّا تجاريًّا)؛ الحالي تجريبي بأسعار حقيقية.
- تحديث `PROJECT_MAP.md` (لا يعكس دورة الرحلة/التفعيل بعد).

---

## 🚀 التشغيل المحلي (عبر WiFi — بدون بناء)
```bash
# شغل كل أمر في طرفية منفصلة
npm run passenger   # يشغل الراكب على 8082 مع مسح الكاش تلقائياً
npm run driver      # يشغل السائق على 8083 مع مسح الكاش تلقائياً
npm run admin       # http://localhost:3002 (متصفّح)
```

### خطوات الاتصال من الهاتف:
1. تأكد أن الهاتف والحاسوب على **نفس الشبكة (WiFi)**.
2. في تطبيق الـ dev-client على الهاتف → اضغط **Enter URL manually**.
3. أدخل: `http://<IP الحاسوب>:8082` للراكب، و `8083` للسائق.
4. أو امسح **QR Code** الذي يظهر في الطرفية مباشرة.

- القاعدة الذهبية: **الراكب = 8082، السائق = 8083 دائمًا**.
- إن فشل الاتصال رغم Wi‑Fi: اسمح لـ Node.js عبر جدار حماية Windows (Private networks).
- لإيقاف الخادم: `Ctrl+C` في الطرفية.
- فحص الأنواع: `npm run typecheck` من الجذر.

## 🧪 اختبار تدفّق التفعيل + الرحلة (بعد 0034 + 0035)
1. سجّل راكبة جديدة → أكّدي البريد → شاشة «قيد المراجعة».
2. اللوحة → الركاب → «تفعيل» (حوار تأكيد) → الراكبة «تحديث الحالة» → الرئيسية.
3. اطلبي رحلة → السائقة تقبل → «وصلت لنقطة الالتقاط» → «بدء» → «إنهاء» → الراكبة تنتقل للتقييم → دفع.

## 📋 معمارية مهمّة
- monorepo (npm workspaces): `apps/{passenger,driver,admin}` + `packages/{shared-ui,shared-types,i18n}`. كلاهما Expo على **SDK 54** (لا تُرجِع الراكب لـ53 — يكسر hoisting).
- مشروع أمانة **منفصل تمامًا** عن مشاريع نواة الستّ (Laravel) — قاعدة واحدة مشتركة، DDL يدويّ عبر SQL Editor، ليس ست نسخ.

# نقطة المتابعة — منصة أمانة (آخر تحديث: 15-07-2026 ليلًا)

مرجع تسليم بين الأجهزة. **اقرأه أولًا عند استئناف العمل**، ثم `PROJECT_MAP.md` للبنية.

> 🔴 **حالة Git — مهم:** وحدة الدعم + self-service مدفوعة (آخر commit مدفوع: `c7b0e03`).
> **لكن عمل جلسة اليوم _غير مدفوع بعد_:** (أدمن) `DateRangePicker` + `FilterToolbar` على ٥ شاشات
> + مهارات UI/UX، و(سائق) أساس الخريطة (`react-native-maps`+`expo-location`+`map.tsx`+app.json).
> ⇒ **يجب `git add -A && commit && push` قبل الانتقال لجهاز المكتب** وإلا لن تجده هناك.

> ⚠️ **على جهاز المكتب قبل أي شيء:**
> 1. `git pull`.
> 2. `npm install` من جذر المستودع — يشغّل `postinstall` (رقعة css-interop
>    `patches/react-native-css-interop+0.2.6.patch`، ضرورية لعدم انهيار السائق على New Arch)
>    ويجلب اعتماديات الخريطة الجديدة.
> 3. تطبيق السائق: `cd apps/driver && npx expo start --clear`.

---

## 🔴 هجرات Supabase — SQL Editor → Run (كلها idempotent، شغّل ما لم يُطبَّق)

| الهجرة | ماذا تفعل | الحالة |
|---|---|---|
| `0022_driver_kyc_fields` | أعمدة KYC: `vehicle_year`/`national_id_number`/`vehicle_registration_number`/`car_photo_url` | تحقّق/شغّل |
| `0023_driver_rejection_reason` | عمود `rejection_reason` (يصل للسائقة) | تحقّق/شغّل |
| `0024_fix_driver_user_type` | إصلاح user_type + backfill السائقين | تحقّق/شغّل |
| `0025_driver_kyc_submitted_at` | عمود `kyc_submitted_at` (مسودّة مقابل مُرسَل) — **إلزامي** لتدفّق KYC | تحقّق/شغّل |
| `0026_support_module` | جداول الدعم + ترقيم + ٥ حالات + استبيان + bucket `avatars` | ✅ طُبِّقت |
| `0027_support_notifications_realtime` | trigger إشعار التذكرة + **تفعيل Realtime** للجرس والقائمة | **شغّلها** |
| `0028_ticket_assign_and_cancel` | إلغاء العميل (عند «جديد» فقط) + التخصيص/التقدّم التلقائي عبر trigger | **شغّلها** |

بعد 0027 أعد تحميل اللوحة مرّة → تصل إشعارات التذاكر/التسجيل **حيًّا بلا Reload**.

---

## ✅ ما أُنجز اليوم (كود مدفوع، `tsc` نظيف: driver + admin + shared-types)

### تطبيق السائق
- **إصلاح انهيار css-interop** على New Arch (رقعة `stringify` + جعل `active:` ثابتة) — كان يُسقط التطبيق بعد التسجيل.
- **مسودّة مقابل مُرسَل** (`kyc_submitted_at`): رفع الصور = مسودّة؛ «إرسال للتدقيق» فقط يُدخِلها المراجعة. حفظ تلقائي للحقول.
- **الإعدادات:** تبديل المظهر (فاتح/داكن/تلقائي) + اللغة (ع/EN مع إعادة تشغيل لقلب RTL) — `src/lib/preferences.tsx`.
- **تبويب «حسابي»:** تعديل الاسم/الجوال + **رفع الأفاتار** (`src/lib/avatar.ts`, bucket `avatars`).
- **الدعم الفني:** قائمة/إنشاء/محادثة/إلغاء/استبيان — `app/support/*`, `src/lib/support.ts`. حدّ ٥ تذاكر مفتوحة (الزر يُعطَّل).
- **الشروط والأحكام** (موافقة إلزامية بالتسجيل) + شاشة **«حول»** — `app/terms.tsx`, `app/about.tsx`.
- **لوحة لاتينية** (أ→A...) في النموذج — `PLATE_LETTER_LATIN`/`formatPlate` في shared-types.

### لوحة الإدارة — وحدة الدعم الكاملة
- **ترقيم بشري:** `dri/pas/adm` + `YYMM` + تسلسل (`dri26070001`).
- **آلة حالات (٥):** جديد→(ردّ)→قيد العمل→بانتظار الرد→منتهية (نهائية) + ملغاة. مُتحقَّقة عميل+خادم (`TICKET_STATUS_TRANSITIONS`).
- **تخصيص تلقائي** لأول موظف يردّ + **تقدّم تلقائي** open→in_progress (trigger DB).
- **إشعارات حيّة** (Supabase Realtime) للجرس + القائمة.
- **تفاصيل التذكرة = نموذج منبثق** موحّد (الرابط المباشر يُعاد توجيهه بـ `?highlight=`)؛ **للقراءة فقط** عند الإغلاق/الإلغاء.
- **التقييم في القائمة** + **قسم الترتيب** (تاريخ/تقييم/موظف/مستخدم) + بحث برقم التذكرة.

---

## ✅ كالندر مدى التاريخ (منجز — بلا مكتبة)

`apps/admin/src/components/ui/DateRangePicker.tsx` — مكوّن مخصّص كامل: شهران جنبًا لجنب +
معاينات جانبية (اليوم/أمس/آخر أسبوع/آخر شهر/الشهر الماضي/مخصّص) + تطبيق/مسح + إغلاق
بالنقر خارجًا وEscape، RTL + وضع داكن + تركيز مرئي (تطبيقًا لقواعد مهارة UI/UX Pro Max).
مربوط بـ`SupportClient` (فلترة `createdAt` بين تاريخين، تصفير الصفحة عند التغيير). `tsc`+`eslint` نظيفان.

> **مهارة مثبّتة:** `ui-ux-pro-max` + `design-system` + `ui-styling` + `brand` … في
> `~/.claude/skills/` — بيانات مرجعية بلا Python (السكربتات تحتاج Python؛ نقرأ `references/*.md`).

## ✅ شريط الفلاتر الموحّد القابل لإعادة الاستخدام (منجز)

`apps/admin/src/components/ui/FilterToolbar.tsx` — مكوّن **config-driven** يُعاد استخدامه على
أي شاشة: **بحث** (يمينًا) + **ترتيب** (قائمة + اتجاه) + زر **«تصفية»** يطوي/يفتح صفّ **شرائح
فلاتر منسدلة** (`FilterConfig[]`: أيقونة + خيارات + قيمة + مسح X) + عدّاد الفلاتر النشطة +
«مسح الكل» + slot `extraControls` (نضع فيه `DateRangePicker`). RTL + داكن + a11y كامل
(role=listbox/option، Escape، تركيز مرئي). رموز دلالية فقط (ذهبي/أنثراسايت) — بلا hex.
> **مطبَّق على 5 شاشات:** الدعم · السائقين (حالة: نشطة/قيد المراجعة/مسودّة/مرفوضة/محظورة) ·
> الركاب (حالة + ترتيب اسم/تاريخ) · فريق العمل (دور + حالة) · التقييمات (المُقيِّم + النجوم،
> على جدول «آخر التقييمات»). كل شاشة: بحث + ترتيب + شرائح فلاتر، والشريط في **بطاقة مستقلّة
> فوق الجدول** كي تطفو القوائم بلا قصّ. `tsc`+`eslint` نظيفان على كامل الأدمن، والمسارات 200.
> لإعادة الاستخدام على شاشة جديدة: مرّر `search/sort/filters` وربطها بحالة الشاشة.
> بُني بتطبيق مهارات ui-ux-pro-max + design-system + ui-styling + brand.

## ▶️ المهمة التالية (متّفق عليها — المرحلة ب: الخريطة)

**إضاءة الخريطة عبر dev build** (الكود جاهز — انظر قسم «المرحلة ب» أدناه):
1. المالك يجهّز **مفتاح Google Maps** + **حساب Expo**.
2. نستبدل المفتاح في `app.json` ونعمل `eas build --profile development` (APK تطويري) —
   ترسم الخريطة وتُحلّ لوحة المفاتيح/الذاكرة معًا.
3. ثم: زر online/offline حقيقي + استقبال طلبات Realtime + قبول/توجيه + الأرباح/الرحلات.

بالتوازي: القراران المعلّقان أدناه (آلة الحالات / نصّ الشروط) جاهزان للتنفيذ متى حُسِما.

---

## ⏸️ قرارات معلّقة (بانتظار المالك)

1. **آلة الحالات:** هل يُسمح بالإغلاق المباشر من «قيد العمل»؟ وهل يرجع «بانتظار الرد» إلى «قيد العمل» تلقائيًّا عند ردّ العميل؟ وهل إغلاق تلقائي (SLA) بعد صمت العميل؟ (طُبّق فقط: «منتهية» نهائية.)
2. **نصّ الشروط والأحكام النهائي** (قانونيّ) — يوجد نصّ مبدئي في `terms` namespace بـ i18n، يُستبدَل.

## 🗺️ المرحلة ب — بدأت (أساس الخريطة، جاهز للـ dev build)

**مثبّت:** `react-native-maps@1.20.1` + `expo-location@~19.0.8` (SDK 54).
**app.json:** إذن الموقع (`expo-location`) + مكان مفتاح خرائط جوجل:
`android.config.googleMaps.apiKey = "REPLACE_WITH_GOOGLE_MAPS_API_KEY"`.
**شاشة جديدة:** `app/(tabs)/map.tsx` — خريطة + موقع السائقة + زر «اتصال/إيقاف» + إعادة تمركز،
ملفوفة بـ`MapBoundary` (حدّ خطأ) يعرض بديلًا نظيفًا داخل Expo Go بدل التعطّل. + تبويب «الخريطة».
`tsc`/`eslint` نظيفان.

> **⚠️ المتبقّي لإضاءة الخريطة (من المالك):**
> 1. **مفتاح Google Maps** من Google Cloud (فعّل *Maps SDK for Android* + قيّده بحزمة
>    `sa.amana.driver` + SHA‑1 + فوترة) → استبدل `REPLACE_WITH_GOOGLE_MAPS_API_KEY` في app.json.
> 2. **حساب Expo** (مجاني) + `eas build --profile development` → APK تطويري ترسم فيه الخريطة
>    (خرائط جوجل لا تُرسَم داخل Expo Go على أندرويد). هذا الـ build يحلّ أيضًا لوحة المفاتيح والذاكرة.
> **بعدها:** زر online/offline حقيقي + استقبال طلبات Realtime + قبول/توجيه + الأرباح والرحلات.

## 🔜 مؤجّل للمرحلة ب
ميزات القيادة (طلبات/أرباح/رحلات)، `eas build` + توزيع APK، الربط العميق، التحديث الإجباري، إشعارات Push.

---

## 🚀 التشغيل
```bash
# السائق (SDK 54 الآن — Expo Go الحديث)
cd apps/driver && npx expo start --clear     # أو --tunnel من شبكة مختلفة
# الإدارة
cd apps/admin && npm run dev                 # http://localhost:3002
```
- الأنواع: `npm run typecheck` من الجذر (أو `npx tsc --noEmit` داخل كل app).

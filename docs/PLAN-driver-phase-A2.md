# خطة المرحلة أ٢ — تطبيق السائقة (تحسينات + ميزات)

> حالة الاعتماد: مُعتمدة من المستخدم (2026-07-15). الترتيب: أ → ب → ج → د.
> قرار اللغة: نافذة تأكيد + إعادة تشغيل تلقائي (expo-updates).

## السياق
انتهت المرحلة أ (تسجيل + KYC + مسودّة/إرسال + اعتماد إداري). هذه الدفعة تضيف
لمسات احترافية وميزات ذاتية الخدمة للسائقة.

---

## المجموعة أ — تحسينات جمالية (بلا هجرة) ✅ مكتملة
- [x] **أ‑1 لوحة: المقابل اللاتيني.** `PLATE_LETTER_LATIN`/`plateLettersToLatin` في
      `packages/shared-types`؛ يُعرض تحت أحرف اللوحة في `kyc.tsx`. التخزين عربي.
- [x] **أ‑2 فصل أحرف اللوحة في الإدارة** — `formatPlate` (`أ ب ت · ABT · 5560`) في
      `UserDetailsModal`.
- [x] **أ‑3 خلل عرض شاشة «قيد المراجعة»** — كان `border-r-4` على حاوية `rounded-xl`
      يُرسَم كقوس لوني؛ استُبدل بشريط كحليّ + `overflow-hidden` في `pending.tsx`.

## المجموعة ب — إعدادات + بروفايل ✅ مكتملة (عدا إضافات مؤجّلة)
- [x] **ب‑1 تبديل الوضع فاتح/داكن** — `colorScheme` (nativewind) + AsyncStorage عبر
      `src/lib/preferences.tsx`. فوري.
- [x] **ب‑2 تبديل اللغة ع/EN** — `preferences.setLanguage` + `profiles.locale` +
      `I18nManager.forceRTL` + إعادة تشغيل (DevSettings في التطوير / expo-updates في
      الإصدار). نافذة تأكيد في شاشة الحساب. RTL انتقل من i18n.ts إلى preferences.
- [x] **ب‑3 صفحة البروفايل** — تبويب «حسابي» (`app/(tabs)/account.tsx`): الاسم/الجوال
      (تعديل + حفظ في profiles + بيانات المصادقة)، البريد (قراءة)، الإعدادات، خروج.
- [x] تحويل `(tabs)/_layout` إلى تبويبين (الرئيسية/حسابي). `+ expo-updates` مثبّت.
- مؤجّل: رفع صورة الأفاتار، صفَّا «حول» و«الدعم» (يُضافان في المجموعة ج).
- مفاتيح i18n مُضافة: `nav.home/account/logout`, `common.languageRestartHint`,
  `validation.saudiPhone` (ع + EN).

## المجموعة ج — الدعم الفني ✅ مكتملة (RLS موجود، بلا هجرة)
- [x] **ج‑1 قائمة تذاكري + فتح تذكرة** — `app/support/index.tsx` + `new.tsx` +
      `src/lib/support.ts` (حدّ 10 تذاكر مفتوحة، أولوية مشتقّة من النوع).
- [x] **ج‑2 محادثة التذكرة** — `app/support/[id].tsx` (رسائل + رد، تُخفى الداخلية).
- [x] **ج‑3 شاشة «حول»** — `app/about.tsx` (هوية/إصدار/رسالة/مدخل الدعم).
- الحارس: سُمح للسائقة المعتمدة بشاشات `support`/`about` خارج التبويبات.
- مفاتيح i18n: `support.categories/status(مُصحَّح)/ticketClosedHint`, `about.*` (ع+EN).
- ملاحظة: مسارات typedRoutes تُعاد توليدها عند تشغيل Metro؛ استُخدمت الصيغة الكائنية
  للمسار الديناميكي + `as Href` للثابت لاستقلال tsc عن نضارة التوليد.

## المجموعة د — الشروط + إبلاغ النتيجة ✅ مكتملة
- [x] **د‑1 الشروط والأحكام** — شاشة `app/terms.tsx` (ثنائية اللغة، فضاء i18n `terms`)
      + مربّع موافقة إلزامي في التسجيل (الزر معطّل حتى الموافقة) + رابط للشروط.
      تُخزَّن لحظة الموافقة في `user_metadata.terms_accepted_at` (بلا هجرة، بلا مساس
      بمُشغِّل التسجيل الحرج — الإدارة تقرؤها من auth user). الحارس يسمح بـ /terms
      لأي حالة (تُفتح من التسجيل غير المُصادَق ومن «حول»).
- [x] **د‑2 إبلاغ النتيجة** — مُغطّى بالتدفّق القائم: القبول ⇒ التوجيه للرئيسية التي
      تعرض «تم اعتماد حسابك بنجاح»؛ الرفض ⇒ /kyc مع بطاقة سبب الرفض. (شاشة «مبروك»
      مخصّصة وPush مؤجّلان للمرحلة ب.)
- ملاحظة: لم تُنشأ هجرة 0026 — استُعيض عنها بـ user_metadata لتفادي تعديل المُشغِّل.

---

## المجموعة هـ — تطوير الدعم + الأفاتار (طلب لاحق) ✅
- [x] **سبب الخطأ:** جدول `support_tickets` لم يُنشأ (هجرة 0017 لم تُطبّق).
- [x] **هجرة 0026 شاملة** — تُنشئ جداول الدعم إن غابت + التحسينات (مكتفية بذاتها):
  - ترقيم بشري: `dri`/`pas`/`adm` + `YYMM` + تسلسل 4 (trigger + جدول عدّادات ذرّي).
  - 5 حالات: open(جديد)·in_progress(قيد العمل)·resolved(بانتظار الرد)·closed(منتهية)·cancelled(ملغاة).
  - حدّ 5 تذاكر غير مُغلقة. إلغاء العميل + استبيان (تقييم+تعليق) عبر دوال SECURITY DEFINER.
  - bucket `avatars` عام + سياسات (رفع على مجلّد المستخدم فقط).
- [x] **السائق:** `support.ts` (5 حالات، حدّ 5، إلغاء، استبيان)، شاشات (رقم التذكرة،
  معلومات كاملة، زر إلغاء، بطاقة استبيان بعد الإغلاق)، رفع الأفاتار في «حسابي».
- [x] **الإدارة:** shared-types (النوع+التسميات، الحدّ 5)، support.ts (رقم+استبيان+cancelled)،
  عرض الرقم ونتيجة الاستبيان في تفاصيل التذكرة، أيقونة cancelled.
- [x] مفاتيح i18n للحالات الخمس + الإلغاء + الاستبيان (ع+EN).

## هجرات مطلوبة
- `0025_driver_kyc_submitted_at.sql` — **مطبّقة؟** (مسودّة/إرسال). ⚠️ يجب تنفيذها.
- `0026_terms_accepted_at.sql` — للمجموعة د.

## ملاحظات
- kyc_submitted_at = مصدر الحقيقة لطابور المراجعة (مسودّة مقابل مُرسَل).
- رقعة `patches/react-native-css-interop+0.2.6.patch` تمنع انهيار تحذير css-interop
  على New Arch — تُطبَّق تلقائيًّا عبر postinstall.
